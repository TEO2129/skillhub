server.port=8080

# H2 en mémoire (pour démarrer sans MySQL)
spring.datasource.url=jdbc:h2:mem:authdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Console H2 accessible sur http://localhost:8080/h2-console
spring.h2.console.enabled=true

# Logging dans un fichier
logging.file.name=logs/auth-server.log
logging.level.com.example.auth=DEBUG

# =============================================
# Pour utiliser MySQL à la place de H2 :
# Commenter les 4 lignes H2 ci-dessus et
# décommenter les lignes suivantes :
# =============================================
# spring.datasource.url=jdbc:mysql://localhost:3306/authdb?createDatabaseIfNotExist=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=votre_mot_de_passe
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect